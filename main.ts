input.onButtonPressed(Button.A, function () {
    platformer.fee(1, MyEnum.Three, sprite)
})
function doSomething (num: number, text: string, bool: boolean, list: any[], sprite: game.LedSprite, image: Image) {
	
}
let sprite: game.LedSprite = null
sprite = game.createSprite(2, 2)
let list = ["a"]
if (convertToText(list).includes("")) {
	
}
if (true) {
	
} else {
	
}
